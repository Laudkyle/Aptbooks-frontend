import React from 'react';
import { useQuery } from '@tanstack/react-query';
import { AlertTriangle, CheckCircle2, ShieldCheck, XCircle } from 'lucide-react';
import { ContentCard } from '../../../../../../shared/components/layout/ContentCard.jsx';
import { Badge } from '../../../../../../shared/components/ui/Badge.jsx';
import { EvatShell, titleCase, useGhanaAdvancedApi } from './_advancedUi.jsx';

export default function GhanaEvatOverview(){const api=useGhanaAdvancedApi();const q=useQuery({queryKey:['gra','evat','readiness'],queryFn:()=>api.fiscalization.getReadiness()});const r=q.data;return <EvatShell title="GRA E-VAT Readiness" subtitle="Operational readiness for AptBooks fiscal documents, devices, offline continuity and the GRA adapter boundary.">{r?<><div className="grid gap-4 lg:grid-cols-4"><ContentCard className="lg:col-span-2"><div className="flex items-center justify-between"><div><div className="text-xs font-semibold uppercase text-slate-500">E-VAT readiness</div><div className="mt-2 text-5xl font-bold">{r.score}%</div><div className="mt-2 text-sm text-slate-500">Adapter mode: <strong>{titleCase(r.settings?.adapter_mode)}</strong> · onboarding: <strong>{titleCase(r.settings?.onboarding_status)}</strong></div></div><ShieldCheck className="h-12 w-12 text-brand-primary"/></div><div className="mt-5 h-2 rounded-full bg-slate-100"><div className="h-full rounded-full bg-brand-primary" style={{width:`${r.score}%`}}/></div></ContentCard><ContentCard><div className="text-xs uppercase text-slate-500">Devices</div><div className="mt-2 text-2xl font-bold">{r.stats?.devices?.certified??0}/{r.stats?.devices?.total??0}</div><div className="text-xs text-slate-500">Certified / total</div></ContentCard><ContentCard><div className="text-xs uppercase text-slate-500">Queue</div><div className="mt-2 text-2xl font-bold">{r.stats?.queue?.pending??0}</div><div className="text-xs text-slate-500">Pending · {r.stats?.queue?.dead_letter??0} dead letter</div></ContentCard></div><ContentCard title="Readiness checks"><div className="grid gap-3 md:grid-cols-2 xl:grid-cols-3">{Object.entries(r.checks??{}).map(([key,ok])=><div key={key} className="flex items-center justify-between rounded-xl border border-border-subtle p-4"><div className="text-sm font-medium text-slate-800">{titleCase(key)}</div>{ok?<CheckCircle2 className="h-5 w-5 text-emerald-600"/>:<XCircle className="h-5 w-5 text-red-600"/>}</div>)}</div></ContentCard>{r.settings?.adapter_mode==='simulation'?<ContentCard><div className="flex gap-3 rounded-xl bg-amber-50 p-4 text-sm text-amber-900"><AlertTriangle className="h-5 w-5 shrink-0"/><div><strong>Simulation mode.</strong> Simulated fiscal documents are for integration testing and must not be presented as GRA-certified receipts.</div></div></ContentCard>:null}</>:<ContentCard><div className="py-10 text-sm text-slate-500">Loading E-VAT readiness…</div></ContentCard>}</EvatShell>}
